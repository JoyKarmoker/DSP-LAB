x = [0.4 0.9 1.2 1.3 3.4 0.7];
xcof = [2/3, 1];
ycof = [1, 0.5];

n = length(x);

output = zeros(1, n);

for i =1:n
    for j = 2:length(ycof)
        if(i>=j)
            output(i) = 
        end
    end
end
